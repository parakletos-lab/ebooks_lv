# Local-only QA credentials (safe defaults for dockerized dev).
# Do not commit real production credentials here.

# Base URL used by smoke scripts/docs
QA_BASE_URL=http://localhost:8083

# Admin (ebooks.lv dev bootstrap)
QA_ADMIN_USERNAME=admin
QA_ADMIN_PASSWORD=AdminTest123!
QA_ADMIN_EMAIL=admin@example.org

# Deterministic non-admin user created by QA bootstrap script
QA_USER_USERNAME=qa_user
QA_USER_PASSWORD=qa_user123
QA_USER_EMAIL=qa_user@example.test

# Deterministic Mozello order seed (used to mark a book as purchased for QA_USER_EMAIL)
QA_ORDER_MZ_HANDLE=qa-seeded-handle

# Optional override: pick a specific Calibre book id for the seeded order
# QA_ORDER_BOOK_ID=1
