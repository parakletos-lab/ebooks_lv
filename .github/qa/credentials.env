# Copy this file to credentials.env (never commit production secrets)
# Default placeholder test accounts (retain for reference)
TEST_ADMIN_EMAIL=admin@example.test
TEST_ADMIN_PASSWORD=change-me
TEST_USER_EMAIL=user@example.test
TEST_USER_PASSWORD=change-me

# --- Provided Local QA Accounts (real for local/dev only) ---
# These are intentionally weak/simple for dockerized local testing.
# Do NOT reuse in any hosted/staging/prod environment.
# Calibre-Web typically authenticates via username; emails retained for future tests.
QA_ADMIN_USERNAME=admin
QA_ADMIN_PASSWORD=admin123
QA_ADMIN_EMAIL=admin@local.test

QA_USER_USERNAME=deniss
QA_USER_PASSWORD=!Qwer1tY!
QA_USER_EMAIL=deniss.muhla@gmail.com

# Additional environment variables for docker-based QA runs can be appended here.
