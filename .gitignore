# Calibre-Web runtime log files (base + rotated/compressed variants)
config/calibre-web.log*

# If you later need to ignore only numbered rotations but keep the base file tracked,
# use instead:
# config/calibre-web.log.*
# !config/calibre-web.log

.env

# Bytecode / caches
__pycache__/
**/__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Virtual environments
env/
venv/
ENV/

# Test / build
.pytest_cache/

# Editor / OS
*.swp
*~
.DS_Store

# Local DB / runtime (optional; remove if you intentionally track these)
config/*.db
library/metadata.db